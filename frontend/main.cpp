#include "Channel.h"
#include "Graph.h"
#include <chrono>
#include <thread>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

void insertVertices(vector<Channel> channels) {
    rand();
}

vector<Channel> readChannels() {

}

int main() {
    // read in all the channels

    srand(time(0));
    return 0;
}